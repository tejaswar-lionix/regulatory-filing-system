import React, {useState} from 'react';
export const EdgarView: React.FC = () => {
  const [filter,setFilter]=useState('high');
  return <div><h2>EDGAR - EDGAR - submission, acceptance, fees, co</h2><p>submission</p></div>
};
export default EdgarView;
