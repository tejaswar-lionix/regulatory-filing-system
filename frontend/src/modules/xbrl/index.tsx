import React, {useState} from 'react';
export const XbrlView: React.FC = () => {
  const [filter,setFilter]=useState('high');
  return <div><h2>XBRL - XBRL - tagging, taxonomy, validation, re</h2><p>tagging</p></div>
};
export default XbrlView;
